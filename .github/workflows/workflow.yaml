name: Hello World Workflow

on:
  push:
    branches:
      - master

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: run greet
      uses: ./.github/actions/greetings
      with: 
        who-to-greet: 'People'